schema_version = 1
name = "research"
author_handle = "ghost-frame"
author_pubkey = "UNSIGNED"
version = "0.1.0"
license = "Elastic-2.0"

[capability_manifest]
required_tools = ["Read", "Grep", "Glob", "Bash", "WebSearch", "WebFetch"]
network_egress = true
filesystem_scope = "project-only"
memory_required = "none"
memory_required_ops = []
